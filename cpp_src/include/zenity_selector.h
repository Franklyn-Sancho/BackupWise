#ifndef ZENITY_SELECTOR_H
#define ZENITY_SELECTOR_H

#include <string>

// Function declaration for selecting directory using Zenity
std::string select_directory_with_zenity(const std::string& title);

#endif // ZENITY_SELECTOR_H
